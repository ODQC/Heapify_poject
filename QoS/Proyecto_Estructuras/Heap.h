/*#pragma once
#include"lista.h"
#include"Paquete.h"
class Heap
{
private:
	int getIzq(int i) {
		return (2 * i + 1);
	}
	int getDer(int i) {
		return (2 * i + 2);
	}
public:
	void Heapify(lista*,int);
	void Swap();
	void HeapSort(lista);

};*/

